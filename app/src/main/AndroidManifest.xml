<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codechallengestorage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserLogin.ProfileUser"></activity>
        <activity android:name=".UserLogin.HalamanLogin" />
        <activity android:name=".GameView.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameView.LandingPageActivity" />
        <activity android:name=".GameView.MenuPermainan" />
        <activity android:name=".GameView.Pemain1vsPemain2"></activity>
        <activity android:name=".GameView.MainActivity" />
    </application>

</manifest>